domain BookmarkManager {
  state {
    totalBookmarks: number = 0
  }

  action addBookmark() {
    onceIntent {
      when totalBookmarks {
        patch totalBookmarks = add(totalBookmarks, 1)
      }
    }
  }
}
