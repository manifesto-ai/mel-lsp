domain InventoryCatalog {
  type Item = {
    sku: string,
    stock: number
  }

  state {
    inventory: Array<Item> = []
  }

  computed lowStock = filter(inventory, lt($item.stock, 5))
  computed skus = map(inventory, $item.sku)
}
